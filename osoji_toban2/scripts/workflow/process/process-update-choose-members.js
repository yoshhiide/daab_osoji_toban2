'use strict';


// メンバー選出＆選出未済対象者のRedis更新
const updateChooseMembers = ({ workflow }) => {

  return ({ domainId, member, unchosen, choose }) => {
    // TODO:
    // メンバー選出
    // 選出未済対象者の更新
    // return

  };
}

module.exports = updateChooseMembers;
